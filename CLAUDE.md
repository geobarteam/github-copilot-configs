@AGENTS.md

## Claude Code specific

- Prefer skills when available.
- Keep changes minimal and verify with tests before finishing.
- For large tasks, start with a plan.
- Read and investigate files before answering — never speculate about code not opened.
- Use `Result<T>` for all business errors — never throw.
